import 'package:flutter/material.dart';

class SwiperWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Esto debería ser un swiper"),
    );
  }
}
